version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd goocli-test/frontend
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: goocli-test/frontend/.output/public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*