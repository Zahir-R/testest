version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd goocli-test/frontend
        - npm install
    build:
      commands:
        - npm run generate
  artifacts:
    baseDirectory: goocli-test/frontend/.amplify-hosting/static
    files:
      - '**/*'